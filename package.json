{
  "name": "node-starter",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "frontend": "http-server",
    "backend": "json-server -w db.json",
    "dev": "run-p frontend backend"
  },
  "dependencies": {
    "http-server": "^0.12.3"
  },
  "devDependencies": {
    "json-server": "^0.16.3",
    "npm-run-all": "^4.1.5"
  }
}
